#compdef grb

_grb_commands() {
  _values "grb" "new" "push" "mv" "rm" "pull" "help"
}

_grb_local_branches() {
  compadd `git branch --no-color | cut -c3-`
}

if (( CURRENT == 2 )); then
  _grb_commands
else
  _grb_commands
  _grb_local_branches
fi